{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt && python server.py",
  "outputDirectory": "",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server.py"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
